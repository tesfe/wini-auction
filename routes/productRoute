const express = require("express");
const router = express.Router();
const { getProduct, search } = require("../controler/aproject");

router.route("/").get(getProduct).post(search);
module.exports = router;
